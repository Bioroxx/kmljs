import {AltitudeModeEnumType} from './types/altitude-mode-enum-type';

/**
 * Because only one single element (altitudeMode) defines AltitudeModeGroup as a substitutionGroup,
 * AltitudeModeGroup is set to the AltitudeModeEnumType
 */
export type AltitudeModeGroup = AltitudeModeEnumType;